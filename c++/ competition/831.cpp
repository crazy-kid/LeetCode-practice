#include <string>
#include <vector>
#include <iostream>

using namespace std;

int main(int argc, char const* argv[])
{
    /* code */
    string s = "abce";
    int at = s.find("@");
    if (at != string::npos) {}
    return 0;
}
